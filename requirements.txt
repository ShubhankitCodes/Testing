# Everyone installs with: pip install -r requirements.txt

# --- audio/ ---
faster-whisper
piper-tts

# --- retrieval/ ---
sentence-transformers
faiss-cpu

# --- llm/ ---
# The client speaks vLLM's OpenAI-compatible API. Safe to install anywhere —
# needed on every machine, including laptops running the mock backend.
openai

# --- pipeline/ (transport) ---
fastapi
uvicorn
websockets

# --- configs/ ---
pyyaml

# --- shared ---
numpy
